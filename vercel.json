{
  "buildCommand": "cd \"Landing page\" && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "Landing page/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
